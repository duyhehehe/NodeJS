{
  "name": "lesson1",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "beauty": "lint-staged",
    "start": "nodemon --inspect src/index.js",
    "watch": "node-sass --watch src/rsc/scss/app.scss src/public/css/app.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nguyenduy",
  "license": "ISC",
  "lint-staged": {
    "src/**/*.{js,json,scss}": "prettier --single-quote --trailing-comma all --write"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-handlebars": "^7.0.4"
  },
  "devDependencies": {
    "lint-staged": "^13.2.1",
    "morgan": "^1.10.0",
    "node-sass": "^8.0.0",
    "nodemon": "^2.0.21",
    "prettier": "^2.8.7"
  }
}
